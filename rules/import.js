const error = {};

const warn = {
	'first': 'warn',
	'exports-last': 'warn',
	'order': 'warn',
	'no-mutable-exports': 'warn',
};

const off = ['named', 'no-unresolved'];
export default {
	error,
	warn,
	off,
};