const error = {};

const warning = {
	'first': 'warn',
	'exports-last': 'warn',
	'order': 'warn',
	'no-mutable-exports': 'warn',
};

const off = ['named', 'no-unresolved'];

module.exports = {
	error,
	warning,
	off,
};