const error = {};

const warning = {
	'check-tag-names': { definedTags: ['values'] },
	'check-line-alignment': 'always',
	'no-bad-blocks': 'warn',
};

const off = [];

module.exports = {
	error,
	warning,
	off,
};