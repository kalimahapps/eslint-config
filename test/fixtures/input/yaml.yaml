name: test
version: 1.0.0
description: A test package

dependencies:
  - vue
  - react

scripts:
  build: tsc
  test: vitest
  lint: eslint

empty-value:
empty-key:
  sub: value

nested:
  list:
    - item1
    - item2
    - item3

quotes: unquoted string